spring:
  datasource:
    url: jdbc:h2:mem:myDb;DB_CLOSE_DELAY=-1
  mvc:
    view:
      prefix: /WEB-INF/pages/
      suffix: .jsp
  mail:
    host: smtp.forgegrid.com
    username: mock
    password: mock
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    show-sql: true
resetpassword:
  domain: localhost
  email: noreply@forgegrid.com
  port: 8080
front:
  web:
    url: http://localhost:3000